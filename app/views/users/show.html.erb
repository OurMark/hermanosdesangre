<div class="columna col_der">
  <div class="col_div perfil">
      <div class="Secciones">
          <div class="seleccionada"> <!-- Perfil -->
              <h1><strong><%= current_user.name %> <%= current_user.surname %></strong></h1>
              <h3><%= current_user.dni %></h3>
              <h2><%= current_user.bloodtype %></h2>
              <h4>Fecha de Nacimiento:</h4>
              <p><%= current_user.birthDate %></p>
              <h4>Sexo:</h4>
              <p><%= current_user.gender %></p>
              <h4>Ubicación:</h4>
              <p><%= current_user.user_locale.city %>, <%= current_user.user_locale.state %>, <%= current_user.user_locale.country %></p>
              <h4>E-mail:</h4>
              <p><%= current_user.email %></p>
              <h4>Contraseña:</h4>
              <p>********</p>
              <%= link_to "CAMBIAR DATOS", edit_user_registration_path, class: "cambiar", style: "left: 250px" %>
          </div>
      </div>
      <%= render partial: 'badges', locals: { badges: current_user.badges } %>
      <h4><a href="@">Próximo turno: <strong>Fecha hora, lugar</strong> - Ver todos.</a></h4>
  </div>
</div>
<div class="columna col_izq">
  <% if current_user.ongs.any? %>
    <% current_user.ongs.each do |ong| %>
      <%= render partial: 'ong_list', locals: {ong: ong} %>
    <% end %>
  <% else %>
    <%= render partial: 'shared/gota_donar' %>
  <% end %>
  <%= link_to "Registrar mi Banco de Sangre u ONG de donación", new_user_ong_path(current_user),{ :class =>"botonONG"}%>
</div>
